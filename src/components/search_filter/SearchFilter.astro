---
import SearchInput from './SearchInput.astro';
import SelectInput from './SelectInput.astro';
---

<style>
  .search-filter-container {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 12px;
    width: 100%;
    max-width: 800px; /* 🔹 Aumenta el ancho en pantallas grandes */
    margin: 0 auto;
    padding: 10px;
  }

  .search-filter-container > * {
    flex: 1; /* 🔹 Hace que los elementos crezcan dinámicamente */
  }

  .search-filter-container > SelectInput {
    flex: 0.3; /* 🔹 Reduce el tamaño del selector */
    min-width: 120px; /* 🔹 Evita que se haga muy pequeño */
  }

  .waterfall-container {
    text-align: center;
    margin-bottom: 20px;
    margin-top: 80px;
  }

  .waterfall-title {
  font-size: 80px; /* 🔹 Aumenta el tamaño del texto */
  font-weight: 500; /* 🔹 Hace que se vea más sólido */
  font-family: 'Waterfall', cursive;
  color: black;
  margin-bottom: 16px;
  margin-top: 16px; /* 🔹 Un poco más de espacio debajo */
}

  @media (max-width: 768px) {
    .search-filter-container {
      flex-direction: column;
      width: 90%; /* 🔹 Ajuste de ancho para pantallas pequeñas */
    }

    .search-filter-container > * {
      width: 100%; /* 🔹 Ocupa todo el ancho disponible */
    }
  }

  /* Media query para pantallas medianas */
  @media (min-width: 768px) and (max-width: 1023px) {
    .waterfall-title {
      margin-top: 0px;
      font-size: 70px;
    }
  }

  @media (min-width: 360px) and (max-width: 767px) {
    .waterfall-title {
      font-size: 50px;
      margin: 0 20px;
    }
  }
</style>

<div class="waterfall-container">
  <h2 class="waterfall-title">¡Descubre nuestros deliciosos productos!</h2>
</div>


<div class="search-filter-container">
  <SearchInput />
  <SelectInput />
</div>
