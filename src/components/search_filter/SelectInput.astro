<style>
  .select-container {
    position: relative;
    display: flex;
    align-items: center;
    width: 220px; /* 🔹 Manteniendo un ancho adecuado */
    height: 55px; /* 🔹 Igualando la altura de SearchBar */
    background: #f5ebde;
    border: 1px solid #c4a88a;
    border-radius: 20px; /* 🔹 Igualando el radio de borde */
    padding: 0 15px;
    font-family: 'Montserrat', sans-serif;
    box-shadow: 0px 4px 10px rgba(0, 0, 0, 0.1);
  }

  .select-container select {
    width: 100%;
    height: 100%;
    border: none;
    background: transparent;
    font-size: 16px;
    color: #4a4a4a;
    font-family: inherit;
    appearance: none;
    outline: none;
    padding-right: 24px;
    cursor: pointer;
  }

  .select-container::after {
    content: "▼";
    position: absolute;
    right: 15px;
    font-size: 14px;
    color: #4a4a4a;
    pointer-events: none;
  }

  .select-container:hover {
    box-shadow: 0px 8px 20px rgba(0, 0, 0, 0.2);
  }

  /* 🔹 Estilos para el menú desplegable */
  .select-container select option {
    background: #f5ebde; /* Fondo que coincide con el del contenedor */
    color: #4a4a4a;
    font-size: 16px;
    padding: 10px;
    border-bottom: 1px solid #c4a88a;
  }

  .select-container select option:hover {
    background: #f5ebde; /* Mismo fondo */
    color: #8B4513; /* Cambiar a un color más oscuro */
  }

  .select-container select option:checked {
    background: #c4a88a; /* Fondo cuando la opción está seleccionada */
    color: white;
    font-weight: bold;
  }
</style>

<div class="select-container">
  <select>
    <option value="">Seleccionar...</option>
    <option value="1">Opción 1</option>
    <option value="2">Opción 2</option>
    <option value="3">Opción 3</option>
  </select>
</div>
